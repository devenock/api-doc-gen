{
  "permissions": {
    "allow": [
      "Bash(curl -s http://localhost:8081/)",
      "Bash(curl -s http://localhost:8765/)",
      "Bash(python3 -c ' *)",
      "Bash(# Check if Postman is installed *)",
      "Read(//Applications/**)"
    ]
  }
}
